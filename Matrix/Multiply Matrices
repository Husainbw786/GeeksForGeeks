void multiply(int A[][100], int B[][100], int C[][100], int n)
{
      //add code here.
      int i,j,k;
      for(i=0;i<n;i++)
      {
          for(j=0;j<n;j++)
          {
              C[i][j] = 0;
              for(k=0;k<n;k++)
              {
                  C[i][j] += A[i][k]*B[k][j];
              }
          }
      }
}
