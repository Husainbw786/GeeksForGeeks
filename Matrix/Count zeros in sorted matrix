class Solution{
public:
	int countZeros(vector<vector<int>>A)
	{
		//code here
		int i,j;
		int count = 0;
		for(i=0;i<A.size();i++)
		{
		    for(j=0;j<A.size();j++)
		    {
		        if(A[i][j] == 0)
		        {
		            count++;
		        }
		    }
		}
		return count;
	}
};
