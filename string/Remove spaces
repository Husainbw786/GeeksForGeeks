

class Solution
{
  public:
    string modify (string s)
    {
        //code here.
        string st = "";
        for(int i=0;i<s.length();i++)
        {
            if(s[i] != ' ' )
            {
                st = st + s[i];
            }
        }
        return st;
    }
};
